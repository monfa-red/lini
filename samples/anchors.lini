// Positioning (SPEC §6): side picks an edge, align slides along it, mount
// decides reserve vs overlay. `on` is an overlay that straddles the edge/corner.
// To place text, wrap it in a |plain| and position the box.

padding: 40;

frame |box| {
  width: 240; height: 150; fill: #eee;
  |plain| { at: 0 0; color: green; "C" }
  |plain| { side: top; align: start; mount: on; color: red; "T<" }
  |plain| { side: top; mount: on; color: red; "T" }
  |plain| { side: top; align: end; mount: on; color: red; "T>" }
  |plain| { side: left; mount: on; color: red; "L" }
  |plain| { side: right; mount: on; color: red; "R" }
  |plain| { side: bottom; align: start; mount: on; color: blue; "BL" }
  |plain| { side: bottom; mount: on; color: blue; "B" }
  |plain| { side: bottom; align: end; mount: on; color: blue; "BR" }
}
